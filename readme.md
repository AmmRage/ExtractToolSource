# An exported project from a game resource extract tool
---
## Current status
As those plugins all signed with key, so it won't run successfully. To make that goal, need export all libs referenced.
Tool lazy to do that.